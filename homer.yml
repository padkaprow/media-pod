# Generation of Kubernetes YAML is still under development!
#
# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-3.3.1
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2021-09-18T18:28:00Z"
  labels:
    app: homer
  name: homer
spec:
  containers:
  - command:
    - /bin/sh
    - /entrypoint.sh
    env:
    - name: PATH
      value: /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
    - name: TERM
      value: xterm
    - name: container
      value: podman
    - name: PORT
      value: "8080"
    - name: USER
      value: darkhttpd
    - name: GROUP
      value: darkhttpd
    - name: GID
      value: "911"
    - name: UID
      value: "911"
    image: docker.io/b4bz/homer:latest
    name: homer
    ports:
    - containerPort: 8080
      hostPort: 8080
      protocol: TCP
    resources: {}
    securityContext:
      allowPrivilegeEscalation: true
      capabilities:
        drop:
        - CAP_MKNOD
        - CAP_NET_RAW
        - CAP_AUDIT_WRITE
      privileged: false
      readOnlyRootFilesystem: false
      seLinuxOptions: {}
    volumeMounts:
    - mountPath: /www/assets
      name: homer-config
    workingDir: /
  dnsConfig: {}
  volumes:
  - hostPath:
      path: /srv/containers/homer
      type: Directory
    name: homer-config
status: {}
